[build]
  base = "frontend"        # path to your React app folder
  command = "npm run build"
  publish = "dist"         # Vite outputs to dist, not build

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200